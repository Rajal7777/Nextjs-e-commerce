const StripePayment = ({
    price, orderId, clientSecret
}:{
  price: number;
  orderId: string;
  clientSecret: string;  
}) => {
    return ( <div>sdf</div>
     );
}
 
export default StripePayment;